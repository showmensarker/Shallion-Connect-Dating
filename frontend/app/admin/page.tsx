import { AppShell } from "@/components/AppShell";
const stats=[['Users','1,245'],['Pending reports','18'],['Payments','£2,830'],['Active memberships','321']];
export default function Admin(){return <AppShell title="Admin dashboard" subtitle="Moderation, users, reports, memberships and payment oversight."><div className="stat-grid">{stats.map(([a,b])=><article className="card stat-card" key={a}><span>{a}</span><strong>{b}</strong></article>)}</div><div className="grid-2"><article className="card"><h2>Moderation queue</h2><p>Review profile reports, message reports and blocked-user trends.</p></article><article className="card"><h2>API coverage</h2><p>User management, matches, conversations, messages, payments, memberships, reports and block endpoints are represented in the interface.</p></article></div></AppShell>}
